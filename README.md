# CloudDrive Mini

**Live**: http://localhost:3000

1. Copy `.env.example` → `.env.local`
2. Add Supabase URL + anon key  
3. `npm install && npm run dev`
4. Create "drive" storage bucket in Supabase

